#include <stdio.h>

int main(){
    int n1,n2,resultado; //"declaração de variáveis"
    n1 = 5; //"; faz pular, se sem ele entende na mesma linha, contudo possa dar erro."
    n2 = 10;
    resultado = n1 + n2;
    printf("O resuldado da soma de %d com %d é igual %d \n", n1, n2, resultado); 
    return 0;
}

